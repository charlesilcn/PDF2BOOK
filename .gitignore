# Python
__pycache__/
*.pyc
*.pyo
*.pyd
.pytest_cache/
.coverage
htmlcov/
.ruff_cache/
.mypy_cache/

# Build / packaging
dist/
build/
*.egg-info/
*.egg

# Project-specific: workspace directory (regenerable via `pdf2book ocr --resume`)
workspace/

# Project-specific: old/test intermediate directories (regenerable)
.pdf2book/
.pdf2book_test/

# Project-specific: generated EPUB outputs (regenerable)
*.epub

# Project-specific: source PDF inputs (external, do not commit)
PDFeBOOK/
*.pdf

# Standard folders: keep directory structure visible (content ignored above)
!inbox/.gitkeep
!library/.gitkeep

# Tests (excluded from public repo)
tests/
test_config.yaml

# IDE / editor
.vscode/
.idea/

# OS
.DS_Store
Thumbs.db

# Trae agent workspace (internal, not for public repo)
.trae/
.trae-html-share-packages/

# Understand-anything knowledge graph cache
.understand-anything/

# Uploads directory
.uploads/

# Sensitive files
*.env
.env.*
!.env.example
*.key
*.pem

# Internal session tracking (not for public repo)
showcase/SessionID.md

# Superpowers SDD planning artifacts (development process, not for public repo)
.superpowers/

# Development planning docs (kept locally, not for public repo)
docs/

# Manual E2E test scripts (kept locally, not for public repo; tests/ already ignored)
scripts/

# Development screenshots (not for public repo)
library-page.png
*-screenshot.png
screenshot-*.png
